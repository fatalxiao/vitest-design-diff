// Types
import type { FC } from 'react';

const Component: FC = () => {
    return (
        <div
            style={{
                width: 120,
                height: 40,
                background: '#000',
                borderRadius: 6,
                boxShadow: '0 4px 4px 0 rgba(0,0,0,0.25)',
            }}
        />
    );
};

export default Component;
